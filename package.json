{
  "name": "fala",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node tests/browser/smoke.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "journal": "node scripts/render-journal.mjs",
    "import": "node scripts/import-backup.mjs"
  },
  "devDependencies": {
    "playwright": "^1.63.0",
    "prettier": "3.9.7"
  },
  "description": "Private Brazilian Portuguese voice tutor and notebook. Static app, GitHub as the database.",
  "engines": {
    "node": ">=22"
  }
}
